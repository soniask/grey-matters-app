import {
  StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
  referenceBox: {
    paddingBottom: 2,
    borderBottomWidth: 1,
    borderBottomColor: '#ff404e',
  },
  referenceHeader: {
    fontWeight: 'bold',
    paddingTop: 20,
  },
  references: {
    paddingTop: 10,
  }
})

export default styles;
