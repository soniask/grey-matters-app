import React from 'react';
import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 18,
    textAlign: 'center',
    paddingTop: 20,
    color: '#282828',
  }
})

export default styles;
