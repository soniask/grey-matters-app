import React from 'react'
import {
	Text,
} from 'react-native'

import styles from '../../styles.js'

const Store = () => (
  <Text style={styles.title}>
    Store
  </Text>
)

export default Store;
