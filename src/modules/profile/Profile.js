import React from 'react'
import {
	Text,
} from 'react-native'

import styles from '../../styles.js'

const Profile = () => (
	<Text style={styles.title}>
		Profile
	</Text>
)

export default Profile;
