import React from 'react';
import {
	Text,
} from 'react-native';

import styles from '../../styles.js';

const Settings = () => (
	<Text style={styles.title}>
		Settings
	</Text>
)

export default Settings;