import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  content: {
    marginTop: 20,
    marginLeft: 20,
    marginRight: 20,
  },
  sectionHeader: {
    fontSize: 25,
    color: '#AEAEAE',
    marginBottom: 10,
  }
})

export default styles;
